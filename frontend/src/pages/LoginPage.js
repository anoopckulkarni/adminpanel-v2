import React from 'react';
import Login from '../components/auth/Login'; // Go up one level to 'src', then into 'components', then 'auth'

const LoginPage = () => {
    return <Login />;
};

export default LoginPage;