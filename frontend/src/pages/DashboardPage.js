import React from 'react';
import Dashboard from '../components/dashboard/Dashboard'; // Go up one level to 'src', then into 'components', then 'dashboard'

const DashboardPage = () => {
    return <Dashboard />;
};

export default DashboardPage;